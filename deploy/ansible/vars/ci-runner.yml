# CI runner configuration
# Currently runs on ten alongside k3s; will move to dedicated machine later.
# Loaded as extra vars: -e @vars/ci-runner.yml

# CI log shipping config (ciLogs) is in production.yml under monitor_values.elk
# Runner container.options (--log-driver=fluentd) is in setup-forgejo.yml
---
{}
