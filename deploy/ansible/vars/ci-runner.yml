# CI runner configuration
# Currently runs on ten alongside k3s; will move to dedicated machine later.
# Loaded as extra vars: -e @vars/ci-runner.yml

# CI log shipping to ELK (requires elk.enabled=true on the k3s cluster)
# Note: monitor_values from production.yml is merged with this via Ansible hash_behaviour
# Since hash merging is deprecated, we use combine filter in the deploy role instead.
# For now, ciLogs must be in production.yml under monitor_values.elk
